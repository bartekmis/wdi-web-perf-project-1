type SectionSettings = {
  bgColour: number,
  paddingTop: number,
  paddingBottom: number,
  containerSize: number,
  fontSize: number,
  textAlignment: number,
}

type FormSettings = {
  id: number,
  bgColour:  number,
  btnColour: number,
  title: string,
  title_size: number,
  title_tag: string,
  description: string,
  footer: string,
}

export type { SectionSettings, FormSettings };